class UserService
{
	constructor(userEntity)
	{
		Object.assign(this,userEntity);   //合并对象，相当于继承了所有的属性（没有继承方法）
	}
	getVersion()
	{
		console.log("1.0");
	}
}

module.exports = UserService;  //注意这种导出方式才是正确的